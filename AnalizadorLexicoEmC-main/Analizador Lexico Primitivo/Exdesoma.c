/*
char comp[200] = "";
    int i, j;

    for (i = 0; i < sizeof(alfa); i++) {
        comp[i] = alfa[i];
    }

    j = i;
    for (i = 0; i < sizeof(num); i++) {
        comp[j+i] = num[i];
    }

    j += i;
    for (i = 0; i < sizeof(var); i++) {
        comp[j+i] = var[i];
    }

    j += i;
    for (i = 0; i < sizeof(opm); i++) {
        comp[j+i] = opm[i];
    }

    j += i;
    for (i = 0; i < sizeof(ces); i++) {
        comp[j+i] = ces[i];
    }

    j += i;
    for (i = 0; i < sizeof(par); i++) {
        comp[j+i] = par[i];
    }

    j += i;
    for (i = 0; i < sizeof(cha); i++) {
        comp[j+i] = cha[i];
    }

    j += i;
    for (i = 0; i < sizeof(con); i++) {
        comp[j+i] = con[i];
    }

    printf("%s", comp);

    return 0;
}
*/
